import org.springframework.web.client.RestTemplate;

public class RestTemplateTrustFixer {
    public RestTemplate fix(RestTemplate toFix) {
        // toFix.setRequestFactory(new );

        return toFix;
    }
}
